{
  target: {
    Data: {
      parentEntities: [
        {
          childrenConnectionNoResolve: {
            totalCount: 2,
            items: [
              {
                property: Value1
              }
            ]
          }
        }
      ]
    }
  },
  sql: {
    Text:
select   p.Id,
         c.Id,
         c.ParentId,
         c.Property
from     ParentEntities as p
         left outer join
         ChildEntities as c
         on p.Id = c.ParentId
order by p.Property,
         p.Id,
         c.Property
  }
}