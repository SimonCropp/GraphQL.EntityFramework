{
  target: {
    Data: {
      parentEntities: []
    }
  },
  sql: {
    Text:
select   p.Id,
         p.Property
from     ParentEntities as p
where    p.Property = @p
order by p.Property,
    Parameters: {
      @p: {
        Value: '; DROP TABLE ParentEntities; --,
        Size: 4000,
        IsNullable: true
      }
    }
  }
}