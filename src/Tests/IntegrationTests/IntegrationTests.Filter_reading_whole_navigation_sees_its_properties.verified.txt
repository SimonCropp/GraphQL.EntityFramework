{
  target: {
    Data: {
      childEntities: [
        {
          property: Allowed
        }
      ]
    }
  },
  sql: {
    Text:
select   c.Id,
         p.Id,
         p.Property,
         c.ParentId,
         c.Property
from     ChildEntities as c
         left outer join
         ParentEntities as p
         on c.ParentId = p.Id
order by c.Property
  }
}