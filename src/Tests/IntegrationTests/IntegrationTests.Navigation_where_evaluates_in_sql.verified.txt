{
  target: {
    Data: {
      parentEntities: [
        {
          children: [
            {
              property: Value1
            }
          ]
        }
      ]
    }
  },
  sql: {
    Text:
select   p.Id,
         c0.Id,
         c0.ParentId,
         c0.Property
from     ParentEntities as p
         left outer join
         (select c.Id,
                 c.ParentId,
                 c.Property
          from   ChildEntities as c
          where  c.Property = @p) as c0
         on p.Id = c0.ParentId
order by p.Property,
         p.Id,
         c0.Id,
    Parameters: {
      @p: {
        Value: VALUE1,
        Size: 4000,
        IsNullable: true
      }
    }
  }
}