{
  target: If `skip` is used an `orderBy` must be specified. Field: parentEntities,
  sql: {
    Text:
select   p.Id,
         p.Property
from     ParentEntities as p
order by p.Property
offset @p rows,
    Parameters: {
      @p: 1
    }
  }
}