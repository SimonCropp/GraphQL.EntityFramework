{
  target: {
    Data: {
      childEntities: [
        {
          property: Child1
        }
      ]
    }
  },
  sql: {
    Text:
select   c.Id,
         c.ParentId,
         c.Property
from     ChildEntities as c
         left outer join
         ParentEntities as p
         on c.ParentId = p.Id
where    p.Id is not null
order by c.Property
  }
}