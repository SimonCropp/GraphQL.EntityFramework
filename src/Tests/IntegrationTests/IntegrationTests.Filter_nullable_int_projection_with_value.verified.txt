{
  target: {
    Data: {
      parentEntitiesFiltered: [
        {
          property: Parent,
          children: [
            {
              property: HasAge,
              nullableAge: 30
            }
          ]
        }
      ]
    }
  },
  sql: {
    Text:
select   f.Id,
         f0.Id,
         f0.NullableAge,
         f0.Property,
         f.Property
from     FilterParentEntities as f
         left outer join
         FilterChildEntities as f0
         on f.Id = f0.ParentId
order by f.Property,
         f.Id,
         f0.Id
  }
}