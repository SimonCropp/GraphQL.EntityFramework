{
  target:
{
  "data": {
    "parentEntities": [
      {
        "id": "Guid_1",
        "simplifiedChildProperties": [
          "EMPTY",
          "EMPTY"
        ]
      }
    ]
  }
},
  sql: {
    Text:
select   p.Id,
         c.Id
from     ParentEntities as p
         left outer join
         ChildEntities as c
         on p.Id = c.ParentId
order by p.Property, p.Id, c.Id
  }
}
