{
  target: {
    Data: {
      parentEntities: [
        {
          property: Value1,
          children: [
            {
              property: Child1
            }
          ]
        }
      ]
    }
  },
  sql: {
    Text:
select   p.Id,
         p.Property,
         c.Id,
         c.ParentId,
         c.Property
from     ParentEntities as p
         left outer join
         ChildEntities as c
         on p.Id = c.ParentId
where    p.Id = @p1
order by p.Property,
         p.Id,
         c.Id,
    Parameters: {
      @p1: Guid_1
    }
  }
}