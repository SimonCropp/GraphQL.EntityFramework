{
  target:
{
  "data": {
    "parentEntities": [
      {
        "property": "Parent1",
        "children": [
          {
            "property": "Child1"
          }
        ]
      }
    ]
  }
},
  sql: {
    Text:
select   p.Id,
         p.Property,
         c.Id,
         c.Property
from     ParentEntities as p
         left outer join
         ChildEntities as c
         on p.Id = c.ParentId
order by p.Property, p.Id, c.Id
  }
}