{
  target: {
    Data: {
      parentEntities: [
        {
          children: [
            {
              property: Value2
            }
          ]
        }
      ]
    }
  },
  sql: {
    Text:
select   p.Id,
         c0.Id,
         c0.ParentId,
         c0.Property
from     ParentEntities as p
         left outer join
         (select c.Id,
                 c.ParentId,
                 c.Property
          from   ChildEntities as c
          where  c.Property is not null
                 and c.Property like @p_startswith escape N'\') as c0
         on p.Id = c0.ParentId
order by p.Property,
         p.Id,
         c0.Property,
    Parameters: {
      @p_startswith: {
        Value: Value%,
        Size: 4000,
        IsNullable: true
      }
    }
  }
}