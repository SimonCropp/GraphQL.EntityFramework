{
  target: {
    Data: {
      parentEntities: [
        {
          children: [
            {
              id: Guid_1
            }
          ]
        }
      ]
    }
  },
  sql: {
    Text:
select   p.Id,
         c0.Id,
         c0.ParentId
from     ParentEntities as p
         left outer join
         (select c.Id,
                 c.ParentId
          from   ChildEntities as c
          where  c.Property = @p) as c0
         on p.Id = c0.ParentId
order by p.Property,
         p.Id,
         c0.Id,
    Parameters: {
      @p: {
        Value: Value1,
        Size: 4000,
        IsNullable: true
      }
    }
  }
}